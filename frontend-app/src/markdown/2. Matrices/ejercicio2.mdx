import { Button, Input } from '@chakra-ui/react'
import testImg from '../../assets/test.png?url'

# Ejercicio 2 – Matrices

Operaciones básicas (suma, multiplicación).

<Button onClick={() => alert('¡Matrices 2 clic!')}>Clic Matrices 2</Button>

<Input placeholder="Entrada Matrices 2" mt={4} />
